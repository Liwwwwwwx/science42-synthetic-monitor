# Science42 平台返回内容专项记录

## 1. 基本信息

- 测试环境：`http://192.168.0.112:23191`
- 测试日期：2026-07-27
- 测试类型：平台核心流程自动化测试——记录实际返回内容
- 测试工具：Playwright + Chromium
- 原始数据：`artifacts/response-content.json`

本次脚本依次发送 10 条固定问题，记录页面返回的助手文本、返回耗时和最终状态。由于测试环境当时提示“最近对话已达上限 100 条，请先删除后再新建”，本次采集在已有会话中完成，未继续创建新会话。

## 2. 返回内容明细

| 序号 | 测试问题 | 实际捕获的返回内容 | 耗时 | 状态 |
|---:|---|---|---:|---|
| 1 | `3.6×10^5 / 1.2×10^3，只输出结果。` | `300` | 4357 ms | 完成 |
| 2 | `125 + 378 等于多少？只输出结果。` | `503` | 3325 ms | 完成 |
| 3 | `12 × 13 等于多少？只输出结果。` | `156` | 4002 ms | 完成 |
| 4 | `水的化学式是什么？简短回答。` | `H₂O` | 6137 ms | 完成 |
| 5 | `地球绕什么运行？简短回答。` | `地球绕太阳运行。` | 4794 ms | 完成 |
| 6 | `什么是机器学习？用一句话说明。` | `机器学习是人工智能的一个分支，它通过算法` | 4408 ms | 已返回，但采集文本不完整 |
| 7 | `请严格只输出 JSON：{"name":"Zhang San","age":18}` | 未在 75 秒内捕获到对应回答 | 75215 ms | 超时 |
| 8 | `请用三点列出：测试、记录、复盘。` | 未在 75 秒内捕获到对应回答 | 75352 ms | 超时 |
| 9 | `为什么需要保存测试日志？用一句话说明。` | `保存测试日志是为了记录实验全过程的详细数据与现象，以便后续进行结果复现、问题排查及数据分析。` | 632 ms | 完成 |
| 10 | `解释 P95，限 20 字。` | `P95 指 95% 的请求响应时间低于该值。` | 1504 ms | 完成 |

## 3. 结果统计

- 成功捕获完整或可用返回内容：8/10
- 未在脚本超时时间内捕获返回内容：2/10
- 返回内容采集成功率：80%
- 已捕获内容的耗时范围：632–6137 ms
- 失败项均在 75 秒采集上限内未得到对应回答，属于“发送后无可用返回/回答超时”表现，应纳入后续告警和复测范围。

## 4. 结果说明与限制

1. 第 6 条已检测到回答状态完成，但当前页面 DOM 中回答段落只捕获到前半句，因此文档保留实际捕获文本，不将其补写成推测内容。
2. 第 7、8 条在 75 秒内未捕获到对应助手回答，记录为超时；不能据此推断服务端绝对没有生成内容，需要结合网络请求日志或服务端日志进一步定位。
3. 本文档记录的是自动化脚本从页面实际读取到的返回内容，不是人工改写后的标准答案。
4. 原始 JSON 数据保留在 `artifacts/response-content.json`，可用于后续复核和生成其他格式报告。

## 5. 结论

本次自动化采集已形成独立的返回内容记录。数学计算、基础知识、测试日志和 P95 解释等场景能够正常返回；JSON 严格格式和三点列举场景在本轮 75 秒窗口内未捕获到结果，需要继续结合接口级流式日志、浏览器网络记录和服务端日志进行问题定位。
